apiVersion: v1
kind: Pod
metadata:
            name: replica-set-pod
            labels:
                pod-label: pod-label-value
                specific: specific-value
spec:
            containers:
                - name: replica-set-container
                  image: nginx
